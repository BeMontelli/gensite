<!DOCTYPE html>
<html lang="fr">
<head>
	<title>{{ nom_site }}</title>
	<meta charset="utf-8"/>
</head>
<body>
<h1>{{ nom_site }}</h1>
<div>
	{{descriptif}}
</div>

<div>
<?php
	include('configuration/config.php');
	include('includes/connection.php');

	$requete="SELECT COUNT(*) FROM personne";
	$resultats = $connection->query($requete);
	$ligne=$resultats->fetch();

	echo "Il y a actuellement ".$ligne[0]." personnes inscrites";

?>
</div>

</body>
</html>
